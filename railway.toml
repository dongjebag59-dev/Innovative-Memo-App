[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn focus.wsgi --bind 0.0.0.0:$PORT --workers 2 --log-file -"
healthcheckPath = "/accounts/login/"
restartPolicyType = "on_failure"
